// ARCHITECTURE.md - Архитектура приложения

# 🏗️ Архитектура приложения

## Обзор системы

Приложение "Система обучения сотрудников" построено на базе React с использованием Context API для управления состоянием и React Router для навигации.

## Компоненты системы

### 1. AuthContext - Управление состоянием

**Файл:** `src/context/AuthContext.jsx`

**Функции:**
- Управление данными пользователя
- Управление прогрессом курсов
- Сохранение данных в localStorage
- Отслеживание завершённых уроков

**Основные методы:**

```javascript
// Авторизация
login(email, password, fullName)
register(email, password, fullName)
logout()

// Управление профилем
updateProfile(updates)

// Управление курсами
enrollCourse(courseId)
markLessonComplete(courseId, lessonId)
getCourseProgress(courseId)
```

**Структура данных пользователя:**
```javascript
{
  email: string,
  fullName: string,
  coursesProgress: {
    [courseId]: {
      completedLessons: number[],
      totalLessons: number,
      started: boolean
    }
  },
  enrolledCourses: string[],
  createdAt: string
}
```

### 2. Страницы (Pages)

#### Login.jsx - Вход в систему
- Форма для входа с email и паролем
- Валидация данных
- Перенаправление на dashboard
- Ссылка на регистрацию

#### Register.jsx - Регистрация
- Форма регистрации с полными данными
- Проверка совпадения паролей
- Проверка минимальной длины пароля
- Автоматический вход после регистрации

#### Dashboard.jsx - Главная панель
- Приветствие пользователя
- Список всех курсов с их статусом
- Прогресс-бары для каждого курса
- Кнопки для записи/продолжения курсов
- Навигация по приложению

#### Courses.jsx - Список курсов
- Детальное отображение всех курсов
- Содержание каждого курса (список уроков)
- Статистика по курсам
- Кнопки для начала/продолжения обучения
- Отметка о завершении курса

#### CoursePage.jsx - Видео-уроки
- Видео-плеер для просмотра уроков
- Боковая панель со списком уроков
- Навигация между уроками
- Отслеживание просмотра видео
- Отметка уроков как пройденных
- Статистика прогресса

#### Profile.jsx - Личный кабинет
- Просмотр и редактирование профиля
- Общая статистика обучения
- Прогресс по каждому курсу
- Визуализация прогресса с прогресс-барами
- Дата регистрации

### 3. Данные курсов

**Файл:** `src/data/courses.js`

**Структура:**
```javascript
export const courses = [
  {
    id: string,           // уникальный ID курса
    title: string,        // название курса
    lessons: [
      {
        id: number,       // номер урока
        title: string,    // название урока
        video: string     // путь к видео
      }
    ]
  }
]
```

**Доступные курсы:**
- welder (Сварщик) - 3 урока
- manager (Менеджер) - 2 урока
- seller (Продавец) - 3 урока

## Поток данных

### Цикл жизни пользователя

```
1. РЕГИСТРАЦИЯ
   ↓
   Пользователь заполняет форму
   → Создаются данные пользователя
   → Данные сохраняются в localStorage
   → Пользователь авторизуется
   ↓
   
2. ГЛАВНАЯ ПАНЕЛЬ
   ↓
   Пользователь видит доступные курсы
   → Может записаться на курсы
   → Видит прогресс по каждому курсу
   ↓
   
3. ПРОСМОТР КУРСА
   ↓
   Пользователь выбирает курс
   → Видит список уроков
   → Выбирает урок для просмотра
   ↓
   
4. ПРОСМОТР ВИДЕО
   ↓
   Видео-плеер проигрывает видео
   → После завершения видео предлагается отметить как пройдено
   → Прогресс обновляется в localStorage
   ↓
   
5. ПРОФИЛЬ
   ↓
   Пользователь видит общий прогресс
   → Может редактировать профиль
   → Видит детальный прогресс по каждому курсу
```

## Состояние приложения

### Хранилище данных

**localStorage:**
- Ключ: `user`
- Содержимое: JSON с данными пользователя
- Автоматически сохраняется и загружается

**Context State:**
- Текущий пользователь
- Функции для обновления данных

### Обновление состояния

```javascript
// Пример: отметить урок как пройденный
1. Пользователь нажимает кнопку
2. Вызывается markLessonComplete(courseId, lessonId)
3. Функция обновляет массив completedLessons
4. Данные сохраняются в localStorage
5. Компонент перерисовывается
6. UI обновляется
```

## Управление доступом

### Защищённые маршруты

```javascript
<ProtectedRoute>
  <Dashboard />
</ProtectedRoute>
```

### Логика защиты

1. Проверяется наличие `user` в контексте
2. Если `user` не существует → перенаправление на `/login`
3. Если `user` существует → рендер компонента

## Стилизация

### Архитектура CSS

```
styles/
├── Auth.css         # Стили авторизации (350 строк)
├── Dashboard.css    # Стили панели (300 строк)
├── Profile.css      # Стили профиля (320 строк)
├── Courses.css      # Стили курсов (280 строк)
└── CoursePage.css   # Стили видео-страницы (350 строк)
```

### Цветовая схема

- **Основной градиент:** #667eea → #764ba2 (фиолетовый)
- **Фон:** #f5f7fa (светло-серый)
- **Текст:** #333 (темный)
- **Акценты:** #667eea (синий-фиолетовый)
- **Успех:** #4caf50 (зелёный)
- **Ошибка:** #c00 (красный)

### Адаптивный дизайн

**Breakpoints:**
- Desktop: 1200px+
- Tablet: 768px - 1024px
- Mobile: 320px - 768px

## Жизненный цикл компонента (React Hooks)

### useState
- Управление состоянием формы
- Хранение ID текущего урока
- Отслеживание режима редактирования

### useEffect
- Проверка авторизации
- Загрузка данных с localStorage
- Обновление UI при изменении состояния

### useContext
- Доступ к данным пользователя
- Использование функций из AuthContext

### useNavigate
- Навигация между страницами
- Перенаправление при авторизации/выходе

## Обработка ошибок

### Валидация данных

**Форма входа:**
- Проверка на пустые поля
- Валидация email

**Форма регистрации:**
- Проверка на пустые поля
- Совпадение паролей
- Минимальная длина пароля (6 символов)

**Редактирование профиля:**
- Проверка на пустые значения
- Валидация email

### Обработка исключений

```javascript
try {
  login(email, password);
  navigate("/dashboard");
} catch (err) {
  setError("Ошибка при входе");
}
```

## Производительность

### Оптимизации

1. **Мемоизация функций:**
   - useCallback для updateProfile, enrollCourse, и т.д.

2. **Условный рендеринг:**
   - Проверка авторизации перед отображением

3. **Ленивая загрузка:**
   - Загрузка видео при необходимости

4. **localStorage:**
   - Кэширование данных для быстрого доступа

## Тестирование

### Сценарии тестирования

1. **Регистрация нового пользователя**
2. **Вход с существующими учетными данными**
3. **Просмотр доступных курсов**
4. **Запись на курс**
5. **Просмотр видео-уроков**
6. **Отметка уроков как пройденных**
7. **Отслеживание прогресса**
8. **Редактирование профиля**
9. **Выход из системы**
10. **Сохранение данных при перезагрузке**

## Расширяемость

### Как добавить новый курс

1. Отредактируйте `src/data/courses.js`
2. Добавьте новый объект курса
3. Обновите инициализацию в AuthContext
4. Видео будет автоматически доступно

### Как добавить новую страницу

1. Создайте компонент в `src/pages/`
2. Импортируйте в `App.jsx`
3. Добавьте маршрут
4. (опционально) Добавьте CSS в `src/pages/styles/`

## Интеграция с backend

### Требуемые API endpoints

```
POST   /api/auth/register
POST   /api/auth/login
POST   /api/auth/logout

GET    /api/user/profile
PUT    /api/user/profile

GET    /api/courses
GET    /api/courses/:id

POST   /api/courses/:id/enroll
POST   /api/courses/:id/lessons/:lessonId/complete

GET    /api/progress
```

### Миграция на backend

1. Заменить localStorage на API запросы
2. Использовать JWT для аутентификации
3. Добавить error handling для сетевых ошибок
4. Реализовать синхронизацию данных

## Безопасность

### Текущие меры безопасности

- Защищённые маршруты
- Проверка авторизации
- Валидация входных данных

### Рекомендуемые улучшения

- Использование JWT токенов
- HTTPS для всех запросов
- Валидация пароля на сервере
- Два фактор аутентификация
- Шифрование чувствительных данных

## Заключение

Архитектура приложения разработана для:
- ✓ Масштабируемости
- ✓ Поддерживаемости
- ✓ Расширяемости
- ✓ Производительности
- ✓ Пользовательского опыта

Все компоненты тесно интегрированы и работают в гармонии для создания полнофункциональной системы обучения.
