// DEPLOYMENT.md - Развёртывание приложения

# 🚀 Развёртывание приложения

## 1️⃣ Подготовка к развёртыванию

### Проверка проекта

1. **Убедитесь, что проект работает локально:**
```bash
npm install
npm run dev
```

2. **Проверьте консоль браузера на ошибки (F12)**

3. **Протестируйте все функции:**
   - Регистрация
   - Вход
   - Просмотр курсов
   - Просмотр видео
   - Профиль

4. **Соберите проект:**
```bash
npm run build
```

### Проверка сборки

```bash
npm run preview
```

Это покажет вам, как будет выглядеть production версия.

## 2️⃣ Развёртывание на хостинг

### Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ)

**Плюсы:**
- Бесплатный хостинг
- Автоматическое развёртывание из GitHub
- Быстрое выполнение
- Поддержка коастомных доменов

**Шаги:**

1. Создайте аккаунт на [vercel.com](https://vercel.com)
2. Подключите GitHub репозиторий
3. Нажмите "Deploy"
4. Приложение будет доступно на `https://yourusername.vercel.app`

### Вариант 2: Netlify

**Плюсы:**
- Бесплатный хостинг
- Простое развёртывание
- Form handling
- Serverless functions

**Шаги:**

1. Создайте аккаунт на [netlify.com](https://netlify.com)
2. Подключите GitHub репозиторий или загрузите папку `dist`
3. Настройки:
   - Build command: `npm run build`
   - Publish directory: `dist`
4. Нажмите "Deploy"

### Вариант 3: GitHub Pages

**Плюсы:**
- Полностью бесплатно
- Интегрирована с GitHub

**Шаги:**

1. В `package.json` добавьте:
```json
"homepage": "https://yourusername.github.io/repo-name"
```

2. Установите `gh-pages`:
```bash
npm install --save-dev gh-pages
```

3. Добавьте scripts в `package.json`:
```json
"predeploy": "npm run build",
"deploy": "gh-pages -d dist"
```

4. Запустите:
```bash
npm run deploy
```

5. В GitHub Settings → Pages выберите `gh-pages` branch

### Вариант 4: AWS S3 + CloudFront

**Плюсы:**
- Высокая производительность
- Масштабируемость
- Скидки для non-profit

**Шаги:**

1. Создайте S3 bucket
2. Загрузите содержимое `dist/` в bucket
3. Включите статический хостинг в S3
4. (опционально) Добавьте CloudFront для CDN

### Вариант 5: Docker контейнер

**Dockerfile:**
```dockerfile
FROM node:18-alpine as build
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**nginx.conf:**
```nginx
server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html;
  }
}
```

## 3️⃣ Конфигурация для production

### Переменные окружения

Создайте файл `.env.production`:
```
VITE_API_URL=https://api.example.com
VITE_APP_NAME=Employee Learning System
```

Используйте в коде:
```javascript
const apiUrl = import.meta.env.VITE_API_URL;
```

### Оптимизация

1. **Минификация CSS/JS**
   - Автоматически при `npm run build`

2. **Сжатие изображений**
   - Установите `vite-plugin-compression`

3. **Lazy loading**
   - Используйте `React.lazy()` для больших компонентов

### Security Headers

Добавьте в `vercel.json` или `netlify.toml`:

**vercel.json:**
```json
{
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "X-Content-Type-Options",
          "value": "nosniff"
        },
        {
          "key": "X-Frame-Options",
          "value": "DENY"
        },
        {
          "key": "X-XSS-Protection",
          "value": "1; mode=block"
        }
      ]
    }
  ]
}
```

## 4️⃣ Миграция данных

### Если переходите на backend

1. **Создайте API endpoints**
2. **Обновите AuthContext:**
```javascript
const login = async (email, password) => {
  const response = await fetch('https://api.example.com/login', {
    method: 'POST',
    body: JSON.stringify({ email, password })
  });
  const userData = await response.json();
  saveUser(userData);
};
```

3. **Реализуйте JWT токены**
4. **Добавьте error handling**

## 5️⃣ Мониторинг и логирование

### Sentry (отслеживание ошибок)

```bash
npm install @sentry/react
```

```javascript
import * as Sentry from "@sentry/react";

Sentry.init({
  dsn: "https://your-key@sentry.io/project-id",
  environment: "production",
});

export default Sentry.withProfiler(App);
```

### Google Analytics

```javascript
import ReactGA from 'react-ga';

ReactGA.initialize('G-XXXXXXXXXX');
ReactGA.pageview(window.location.pathname);
```

## 6️⃣ Тестирование before production

### Функциональное тестирование

- [ ] Регистрация новых пользователей
- [ ] Вход в систему
- [ ] Просмотр курсов
- [ ] Запись на курс
- [ ] Просмотр видео
- [ ] Отслеживание прогресса
- [ ] Редактирование профиля
- [ ] Выход из системы

### Браузеры

- [ ] Chrome (последняя версия)
- [ ] Firefox (последняя версия)
- [ ] Safari (последняя версия)
- [ ] Edge (последняя версия)

### Устройства

- [ ] Desktop (1920x1080)
- [ ] Tablet (768x1024)
- [ ] Mobile (375x667)

### Производительность

```bash
# Анализ бандла
npm run build -- --analyze
```

## 7️⃣ После развёртывания

### Проверки

1. **Откройте сайт в браузере**
2. **Проверьте консоль браузера (F12) на ошибки**
3. **Попробуйте все основные функции**
4. **Проверьте на разных устройствах**

### Мониторинг

- Отслеживайте ошибки через Sentry
- Анализируйте трафик через Google Analytics
- Проверяйте логи сервера

### Обновление

```bash
# Для простого обновления:
git push  # На Vercel/Netlify это автоматически развёртывает

# Для Docker:
docker build -t app:latest .
docker push your-registry/app:latest
```

## 🔧 Troubleshooting

### Проблема: 404 при обновлении страницы

**Решение:** Добавьте переписку путей в конфигурацию:

**Vercel:**
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

**Netlify (_redirects файл):**
```
/*    /index.html   200
```

### Проблема: localStorage не работает

**Решение:** Проверьте режим инкогнито браузера

### Проблема: Видео не проигрывается

**Решение:**
1. Проверьте CORS политику
2. Убедитесь, что видео доступно
3. Используйте HTTPS для видео

### Проблема: Медленная загрузка

**Решение:**
1. Оптимизируйте видео файлы
2. Используйте CDN
3. Включите кэширование

## 📋 Чеклист перед production

- [ ] Все функции протестированы
- [ ] Нет console.log() в коде production
- [ ] Нет hardcoded URLs, используются переменные окружения
- [ ] Все ошибки обработаны
- [ ] Добавлены security headers
- [ ] HTTPS включён
- [ ] Кэширование настроено
- [ ] Монитор ошибок подключен (Sentry)
- [ ] Analytics подключан (Google Analytics)
- [ ] README/документация актуальна
- [ ] Резервная копия данных сделана
- [ ] Backup план готов

## 📞 Поддержка

Если у вас возникли проблемы при развёртывании:

1. Проверьте документацию вашего хостинга
2. Посмотрите логи (обычно в Dashboard хостинга)
3. Проверьте консоль браузера на ошибки
4. Убедитесь, что все зависимости установлены

## ✅ Готово!

Если вы следовали этому гайду, ваше приложение должно быть успешно развёрнуто! 🎉

Поздравляем! Ваша система обучения сотрудников теперь в сети и готова к использованию!
